const ProductCart = () => {
  // const { id, category, image, price } = props.data;
  // console.log(id);
  return (
    <div>
      <h1>Product Cart</h1>
      <h4>Page</h4>
    </div>
  );
};

export default ProductCart;
